/// Adds two integers and returns the result
int add(int a, int b) {
    return a + b;
}

/// Main entry point for the Crusty example program
void main() {
    let x: int = 10;
    let y: int = 20;
    let sum: int = add(x, y);
}
